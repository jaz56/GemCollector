<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.Button?>

<AnchorPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="gemcollector.entities.GameWinController"
            prefHeight="600" prefWidth="800"
            style="-fx-background-color: linear-gradient(to bottom, #27ae60, #2ecc71);">

    <!-- VICTOIRE Title -->
    <Label text="VICTOIRE ! 🎉"
           layoutX="180" layoutY="120"
           style="-fx-font-size: 72px; -fx-font-weight: bold; -fx-text-fill: white;
                  -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.7), 15, 0.5, 0, 5);" />

    <!-- Score Final -->
    <Label fx:id="finalScoreLabel"
           text="Score Final: 0"
           layoutX="250" layoutY="240"
           style="-fx-font-size: 40px; -fx-font-weight: bold; -fx-text-fill: #f1c40f;" />

    <!-- Message -->
    <Label text="Tous les gems collectés ! Bravo ! 🇹🇳"
           layoutX="180" layoutY="310"
           style="-fx-font-size: 24px; -fx-text-fill: white;" />

    <!-- Bouton REJOUER -->
    <Button text="🔄 REJOUER"
            layoutX="150" layoutY="400"
            prefWidth="180" prefHeight="55"
            style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-background-radius: 12;
                   -fx-background-color: #3498db; -fx-text-fill: white;
                   -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 8, 0.3, 0, 3);"
            onAction="#handleRestartClick"/>

    <!-- ⭐ Bouton MENU -->
    <Button text="🏠 MENU"
            layoutX="350" layoutY="400"
            prefWidth="180" prefHeight="55"
            style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-background-radius: 12;
                   -fx-background-color: #95a5a6; -fx-text-fill: white;
                   -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 8, 0.3, 0, 3);"
            onAction="#handleBackToMenuClick"/>

    <!-- Bouton NIVEAU SUIVANT -->
    <Button text="➡️ NIVEAU SUIVANT"
            layoutX="550" layoutY="400"
            prefWidth="200" prefHeight="55"
            style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-background-radius: 12;
                   -fx-background-color: #e67e22; -fx-text-fill: white;
                   -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 8, 0.3, 0, 3);"
            onAction="#handleNextLevelClick"/>

</AnchorPane>