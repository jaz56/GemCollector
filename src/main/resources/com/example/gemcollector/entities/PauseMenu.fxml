<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.StackPane?>

<StackPane xmlns="http://javafx.com/javafx/17"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="gemcollector.entities.PauseMenuController"
           prefHeight="600" prefWidth="800"
           style="-fx-background-color: rgba(0, 0, 0, 0.75);">

    <!-- VBox centrée avec le menu pause -->
    <VBox alignment="CENTER"
          spacing="35"
          maxWidth="420"
          style="-fx-background-color: rgba(44, 62, 80, 0.98);
                 -fx-padding: 60px 40px;
                 -fx-background-radius: 25px;
                 -fx-border-radius: 25px;
                 -fx-border-color: #3498db;
                 -fx-border-width: 4px;
                 -fx-effect: dropshadow(gaussian, rgba(52, 152, 219, 0.6), 40, 0.4, 0, 0);">

        <!-- TITLE -->
        <Label text="⏸️ PAUSE"
               style="-fx-font-size: 58px;
                      -fx-font-weight: bold;
                      -fx-font-family: 'Arial Black', 'Impact', sans-serif;
                      -fx-text-fill: linear-gradient(to bottom, #ecf0f1, #bdc3c7);
                      -fx-effect: dropshadow(gaussian, rgba(52, 152, 219, 0.7), 18, 0.6, 0, 4);"/>

        <!-- MAIN BUTTONS -->
        <VBox alignment="CENTER" spacing="16">
            <Button fx:id="resumeButton"
                    text="▶️ REPRENDRE"
                    onAction="#resumeAction"
                    prefWidth="280"
                    prefHeight="58"
                    style="-fx-background-color: #27ae60;
                           -fx-text-fill: white;
                           -fx-font-size: 20px;
                           -fx-font-weight: bold;
                           -fx-background-radius: 14;
                           -fx-cursor: hand;
                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 10, 0.4, 0, 4);"/>

            <Button fx:id="restartButton"
                    text="🔄 RECOMMENCER"
                    onAction="#restartAction"
                    prefWidth="280"
                    prefHeight="58"
                    style="-fx-background-color: #3498db;
                           -fx-text-fill: white;
                           -fx-font-size: 20px;
                           -fx-font-weight: bold;
                           -fx-background-radius: 14;
                           -fx-cursor: hand;
                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 10, 0.4, 0, 4);"/>

            <Button fx:id="menuButton"
                    text="🏠 MENU PRINCIPAL"
                    onAction="#backToMenuAction"
                    prefWidth="280"
                    prefHeight="58"
                    style="-fx-background-color: #95a5a6;
                           -fx-text-fill: white;
                           -fx-font-size: 20px;
                           -fx-font-weight: bold;
                           -fx-background-radius: 14;
                           -fx-cursor: hand;
                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 10, 0.4, 0, 4);"/>

            <Button fx:id="quitButton"
                    text="❌ QUITTER"
                    onAction="#quitAction"
                    prefWidth="280"
                    prefHeight="58"
                    style="-fx-background-color: #e74c3c;
                           -fx-text-fill: white;
                           -fx-font-size: 20px;
                           -fx-font-weight: bold;
                           -fx-background-radius: 14;
                           -fx-cursor: hand;
                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 10, 0.4, 0, 4);"/>
        </VBox>

        <!-- LOWER ICON BUTTONS -->
        <HBox alignment="CENTER" spacing="35">
            <Button fx:id="soundButton"
                    text="🔊"
                    prefWidth="80"
                    prefHeight="60"
                    onAction="#toggleSound"
                    style="-fx-background-color: #3498db;
                           -fx-text-fill: white;
                           -fx-font-size: 28px;
                           -fx-cursor: hand;
                           -fx-background-radius: 14;
                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.4), 8, 0.3, 0, 3);"/>

            <Button fx:id="musicButton"
                    text="🎵"
                    prefWidth="80"
                    prefHeight="60"
                    onAction="#toggleMusic"
                    style="-fx-background-color: #9b59b6;
                           -fx-text-fill: white;
                           -fx-font-size: 28px;
                           -fx-cursor: hand;
                           -fx-background-radius: 14;
                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.4), 8, 0.3, 0, 3);"/>
        </HBox>

        <!-- Hint text -->
        <Label text="Appuyez sur ESC pour reprendre"
               style="-fx-font-size: 14px;
                      -fx-text-fill: rgba(236, 240, 241, 0.7);
                      -fx-font-style: italic;"/>

    </VBox>

</StackPane>