<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Slider?>

<VBox xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="gemcollector.entities.OptionsController"
      alignment="TOP_CENTER"
      spacing="30"
      style="-fx-background-color: linear-gradient(to bottom, #e0f0ff, #ffffff);
             -fx-padding: 50;">

    <!-- Titre avec emoji -->
    <Label text="⚙️ OPTIONS"
           style="-fx-font-size:48px; -fx-font-weight:bold; -fx-text-fill:#0059b3;
                  -fx-effect: dropshadow(gaussian, rgba(0,89,179,0.3), 10, 0.3, 0, 3);" />

    <!-- Sous-titre -->
    <Label text="Pac-Man Tunisien - Sidi Bou Said"
           style="-fx-font-size:16px; -fx-text-fill:#3498db; -fx-font-style: italic;" />

    <!-- Musique -->
    <VBox spacing="12" alignment="CENTER">
        <Label text="🎵 Musique de fond"
               style="-fx-font-size:24px; -fx-text-fill:#003366; -fx-font-weight:bold;" />
        <Button fx:id="btnMusic" onAction="#toggleMusic" text="ON"
                prefWidth="140" prefHeight="50"
                style="-fx-background-radius:12; -fx-background-color:#27ae60;
                       -fx-text-fill:white; -fx-font-size:18px; -fx-font-weight:bold;
                       -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 5, 0.3, 0, 2);" />
    </VBox>

    <!-- Sons -->
    <VBox spacing="12" alignment="CENTER">
        <Label text="🔊 Effets sonores"
               style="-fx-font-size:24px; -fx-text-fill:#003366; -fx-font-weight:bold;" />
        <Button fx:id="btnSound" onAction="#toggleSound" text="ON"
                prefWidth="140" prefHeight="50"
                style="-fx-background-radius:12; -fx-background-color:#27ae60;
                       -fx-text-fill:white; -fx-font-size:18px; -fx-font-weight:bold;
                       -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 5, 0.3, 0, 2);" />
    </VBox>

    <!-- Volume -->
    <VBox spacing="12" alignment="CENTER">
        <Label text="🔉 Volume général"
               style="-fx-font-size:24px; -fx-text-fill:#003366; -fx-font-weight:bold;" />
        <HBox spacing="15" alignment="CENTER">
            <Label text="🔇" style="-fx-font-size:20px;" />
            <Slider fx:id="sliderVolume" prefWidth="300" min="0" max="100" value="30"
                    onMouseReleased="#changeVolume"
                    style="-fx-background-radius:10;" />
            <Label text="🔊" style="-fx-font-size:20px;" />
        </HBox>
        <Label fx:id="volumeLabel" text="Volume: 30%"
               style="-fx-font-size:16px; -fx-text-fill:#7f8c8d;" />
    </VBox>

    <!-- Statistiques -->
    <VBox spacing="12" alignment="CENTER"
          style="-fx-background-color: rgba(52, 152, 219, 0.1);
                 -fx-padding: 20; -fx-background-radius: 15;">
        <Label text="📊 Statistiques"
               style="-fx-font-size:24px; -fx-text-fill:#003366; -fx-font-weight:bold;" />
        <Label fx:id="statsLabel" text="Chargement..."
               style="-fx-font-size:14px; -fx-text-fill:#34495e; -fx-text-alignment:center;" />
    </VBox>

    <!-- Boutons de navigation -->
    <HBox spacing="20" alignment="CENTER">
        <Button fx:id="btnReset" onAction="#resetStats" text="🗑️ Réinitialiser Stats"
                prefWidth="220" prefHeight="50"
                style="-fx-font-size:16px; -fx-background-radius:12;
                       -fx-background-color:#e74c3c; -fx-text-fill:white; -fx-font-weight:bold;
                       -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 5, 0.3, 0, 2);" />

        <Button fx:id="btnBack" onAction="#goBack" text="🏠 Retour au Menu"
                prefWidth="220" prefHeight="50"
                style="-fx-font-size:16px; -fx-background-radius:12;
                       -fx-background-color:#3498db; -fx-text-fill:white; -fx-font-weight:bold;
                       -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 5, 0.3, 0, 2);" />
    </HBox>

</VBox>